<Page
    x:Class="TheAsocialNetwork.UI.UWP.Views.LandingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:TheAsocialNetwork.UI.UWP.Controls"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
            Style="{StaticResource TextBlockSmallHeaderStyle}">
            Welcome
        </TextBlock>

        <TextBlock Grid.Row="1"
                   VerticalAlignment="Top"
            Style="{StaticResource TextBlockFullContentStyle}">
           This is your place,
            This is you own private network where there is nobody but you and everything is about you.
            Here you can be yourself, be the one you ever wanted.
            There is nothing but you.
            Keep calm and фостата.
        </TextBlock>

    </Grid>
</Page>
